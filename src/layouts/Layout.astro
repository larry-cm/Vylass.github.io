---
import Header from "../components/Header.astro";
import { ClientRouter } from "astro:transitions";
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta content="IE=edge,chrome=1" http-equiv="X-UA-Compatible" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<meta name="generator" content={Astro.generator} />

		<link rel="icon" type="image/svg+xml" href="/logo.svg" />
		<title>{title}</title>
		<meta
			name="description"
			content="esta pagina web va a ser mi primera prueba administrando bases de datos"
		/>

		<ClientRouter />
	</head>
	<body>
		<Header />
		<main class="max-w-2xl mx-auto">
			<slot />
		</main>
	</body>
</html>
<style is:global>
	:root {
		--bg: orange;
		--h-mini: 50px;
		--accent-gradient: linear-gradient(
			45deg,
			rgb(var(--accent)),
			rgb(var(--accent-light)) 30%,
			white 60%
		);
	}
	html {
		font-family: system-ui, sans-serif;
		background: #13151a;
	}
	body {
	}

	.header {
		background-color: var(--bg);

		nav {
			display: flex;
			justify-content: space-between;
			align-items: center;

			#nav {
				height: 100%;
				display: flex;
				align-items: center;

				img {
					height: calc(var(--h-mini) + 10px);
					min-height: var(--h-mini);
					min-width: calc(var(--h-mini) + 10px);
					margin-right: 01px;
					margin-left: calc(var(--h-mini) - 40px);
				}

				> h1 {
					/* font-family: .6rem !important; */
					text-wrap: nowrap;
				}
			}
		}
		.no-ani-color {
			transition: color 60ms;
		}
	}
</style>
